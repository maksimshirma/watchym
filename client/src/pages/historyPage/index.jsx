import { Layout } from "../../shared";
import { Operations, Accounts } from "../../widgets";

const HistoryPage = () => {
    return (
        <Layout>
            <Accounts />
            <Operations />
        </Layout>
    );
};

export default HistoryPage;
