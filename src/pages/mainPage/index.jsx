import { Unauthorized } from "../../widgets";

const MainPage = () => {
    return (
        <div className="h-full w-full">
            <Unauthorized />
        </div>
    );
};

export default MainPage;
